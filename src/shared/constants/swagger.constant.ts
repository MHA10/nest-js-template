/**
 * Swagger Constants
 *
 * Defines standardized tags and metadata keys used across
 * the Swagger OpenAPI documentation.
 */

/**
 * Endpoint grouping tags for the Swagger UI.
 */
export enum SwaggerTag {
  /** Health checks, diagnostic endpoints, and system status */
  SYSTEM = 'System',
}
